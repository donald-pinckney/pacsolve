# path to a directory with all packages
storage: ../../generated/yarn2/verdaccio/storage
# path to a directory with plugins to include
plugins: ../../generated/yarn2/verdaccio/plugins

web:
  title: Verdaccio
  gravatar: false

auth:
  htpasswd:
    file: ../../generated/yarn2/verdaccio/htpasswd

packages:
  '@*/*':
    # scoped packages
    access: $all
    publish: $all
    unpublish: $all
    proxy: npmjs

  '**':
    access: $all
    publish: $all
    unpublish: $all

server:
  keepAliveTimeout: 60

middlewares:
  audit:
    enabled: true

logs:
  - { type: stdout, format: pretty, level: http }
