(library
  (name testing)
  (modules runner graph)
  (libraries ounit2 extlib str yojson)
  (wrapped false))

(test
  (name test)
  (modules test)
  (libraries unix str testing))

(env (dev (flags (:standard -warn-error -A)))) ; set warnings as "non-errors"


