(library
  (name testing)
  (modules runner graph testutils)
  (libraries ounit2 ounit2.threads extlib str yojson)
  (wrapped false))

(env (dev (flags (:standard -warn-error -A)))) ; set warnings as "non-errors"


